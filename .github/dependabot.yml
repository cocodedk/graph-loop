version: 2
updates:
  # The pip ecosystem is added back with the runtime. Declaring it before there
  # is a Python dependency file makes Dependabot fail on every run, which trains
  # everyone to ignore a red mark.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
